{
  "name": "ssip-functions-api",
  "private": true,
  "version": "1.0.0",
  "description": "Secure Serverless Incident Portal Functions API",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "typescript": "^5.0.0",
    "@types/node": "18.x"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}